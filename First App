import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
    home : Scaffold(
      appBar:AppBar(
        title: Text('Hello, its my first app'),
        centerTitle: true,
      ) ,
      body: Center(
        child: Text('hey app builders'),
      ),
      floatingActionButton: FloatingActionButton(
        child: Text("+"),
      ),
    )
));
